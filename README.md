# K-Balaraju
Performed data cleaning and preprocessing on a dataset using Python. Handled missing values, encoded categorical features, normalized numerical data, detected and removed outliers, and prepared the dataset for machine learning using Pandas, NumPy, and Matplotlib/Seaborn.
